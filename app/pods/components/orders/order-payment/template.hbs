<script src="https://checkout.razorpay.com/v1/checkout.js"></script>
<div class="t-align-c">
  <h2>Payment Confirmation</h2>

  <h4>₹ {{@order.amountDisplay}}</h4>

  <button 
    class="btn btn-danger"
    disabled={{capturePayment.isRunning}}
    {{action (perform payNow)}}
  >
    {{if capturePayment.isRunning 'Paying' 'Pay Now'}}
  </button>

  {{#if payNowLastResult.isError}}
  <div class="red">
    Application Already Exists
  </div>
  {{/if}}
</div>